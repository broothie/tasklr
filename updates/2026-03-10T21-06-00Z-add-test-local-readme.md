# 2026-03-10T21:06:00Z — Add `test:local` note to README

## Summary

Added a short README note recommending `npm run test:local` for a fast local syntax check plus dependency-free tests. This documents the quick local workflow that was exercised in recent runs.

## What I changed

- `README.md` — added a small paragraph under the Tests section describing `npm run test:local` and what it does.

## Why

Several recent updates ran dependency-free checks locally; documenting `npm run test:local` makes that easier for contributors running the repo in constrained environments.

## Next steps

- Continue with small maintenance tasks or run the CI-enabled tests in a network-enabled environment.

Turns used: 2
